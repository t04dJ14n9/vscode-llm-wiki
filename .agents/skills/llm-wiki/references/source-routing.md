# Source routing

Prefer repository-local tools and purpose-built connectors over ad hoc copies.
Read external systems only when authorized. Preserve source identity,
revision/retrieval time, and access boundaries. Never persist credentials,
private identifiers, or redacted values. Never write back to a repository,
document service, chat, or publication target without explicit authority.
