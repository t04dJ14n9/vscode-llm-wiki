{
  "name": "@human-learning/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Human Learning — CLI (hl) for headless vault operations",
  "main": "dist/main.js",
  "bin": {
    "hl": "dist/main.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "pnpm --filter @human-learning/core build && pnpm build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@human-learning/core": "workspace:*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
