{
  "name": "@llm-wiki/core",
  "version": "0.4.5",
  "private": true,
  "description": "LLM Wiki — portable references and PDF discussion persistence",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "pnpm build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
